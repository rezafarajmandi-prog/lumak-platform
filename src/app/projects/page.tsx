import ProjectsContent from './ProjectsContent';

export const metadata = {
  title: 'پروژه‌ها — LUMAK',
  description: 'پروژه‌های نورپردازی معماری توسط LUMAK.',
};

export default function ProjectsPage() {
  return <ProjectsContent />;
}
