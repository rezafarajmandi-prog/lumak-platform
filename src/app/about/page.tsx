export const metadata = {
  title: 'درباره — LUMAK',
  description: 'LUMAK: معماری نور از سال 2020.',
};

import AboutContent from './AboutContent';

export default function AboutPage() {
  return <AboutContent />;
}
